package com.ehan.tutasting.model

import android.os.Parcelable
import kotlinx.parcelize.Parcelize

enum class Screen {
    SPLASH,
    LOADING,
    MAINMENU,
    CHANGEMODE,
    PLAYING,
    SETTINGS,
    PAUSED
}
